{
  "name": "domaine_test_yaroslav",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "shopify theme dev -e development",
    "tw:watch": "npx tailwindcss -i ./assets/tailwind.input.css -o ./assets/tailwind.output.css --watch",
    "concurrently": "concurrently \"npx tailwindcss -i ./assets/tailwind.input.css -o ./assets/tailwind.output.css --watch\" \"shopify theme dev -e development\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
